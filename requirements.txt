Django==5.2.7
numpy==2.3.3
scipy==1.16.2
scikit-learn==1.7.2
xgboost==3.0.5
joblib==1.5.2
pillow==12.0.0

# Production static files
whitenoise==6.9.0

# Production database (PostgreSQL). Local dev falls back to SQLite when
# DATABASE_URL is not set, so these are only exercised in production.
dj-database-url==2.3.0
psycopg[binary]==3.2.9

# Optional production media storage (Cloudinary). Uncomment to enable, then
# set the CLOUDINARY_URL environment variable (see README / settings.py):
# cloudinary==1.44.0
# django-cloudinary-storage==0.3.0
